# - name: Add a line to .bashrc
#   ansible.builtin.lineinfile:
#     path: "{{ ansible_env.HOME }}/.bashrc"
#     line: 'export MY_ENV_VAR="my_value"'
#     create: yes

# - name: Source the updated .bashrc
#   ansible.builtin.shell: |
#     source ~/.bashrc
#   args:
#     executable: /bin/bash

- name: make file and edit line
  become: true
  ansible.builtin.lineinfile:
    path: "{{ ansible_env.HOME }}/testfile.txt"
    line: 'some test'
    create: yes
- name: debug home variable
  debug:
    msg: "{{ ansible_env.HOME }}"